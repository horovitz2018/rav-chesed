[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# הפניית כל הנתיבים ל-index.html (אפליקציית עמוד יחיד)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
