-- ═══════════════════════════════════════════════════════════════
--  רב חסד — אופן תשלום להתחייבות (אשראי / בנק / מזומן)
--  הרץ ב-Supabase: SQL Editor → New query → הדבק → Run
--  מוסיף עמודה בלבד — לא נוגע בנתונים קיימים.
-- ═══════════════════════════════════════════════════════════════

alter table pledges add column if not exists method text default 'bank';
-- ערכים: 'stripe' (אשראי/הו"ק בכרטיס) | 'bank' (העברה בנקאית) | 'cash' (מזומן)
